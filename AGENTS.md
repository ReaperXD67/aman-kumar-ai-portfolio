# Prototype Instructions

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.

## Durable visual direction

- Preserve the cinematic black, industrial, editorial AI-system identity established in `DESIGN.md`.
- Favor a small number of high-impact, technically authored interactions over generic card decoration or scattered effects.
- The portfolio should feel surprising and premium—especially the procedural hero object and project-scroll experience—while remaining responsive, accessible, and honest about project status.
- When pushing the experience further, combine ambitious effects into one coherent authored sequence. The preferred direction is maximal and cinematic—scroll-controlled spatial storytelling, WebGL depth, and project artifacts—without scattering unrelated decorative animations across the page.
- Adjacent major sections must not repeat the same project imagery or interaction anatomy. Treat the cognitive descent as the cinematic overview and the selected-work section as an evidence-inspection surface: architecture path, failure mode, verified proof, and source links.
- The selected-work surface must not feel like a static ledger. Lead with a real project preview, let visitors switch between interface and system anatomy, and reveal proof through an authored “project transmission” interaction. KarixMC is a featured production project and should be presented with its real live-site capture, VPS status, Paper-plugin boundary, and source links.
- Personal positioning is “AI Engineer & Full-Stack Developer,” based in Bengaluru, India, available for AI engineering, full-stack roles, internships, and freelance/contract builds in any work mode.
- As of 2026-09-05, Aman explicitly wants his approved natural portrait visible in the portfolio and GitHub. Reveal the real photograph through an authored top-to-bottom pixel assembly; keep it clear and recognizable after the short entrance. The identity frame offers Person / System views, retaining the procedural Systems Kernel as its second face. A compact portrait entry in the hero makes the person discoverable immediately.
- Hero and identity procedural objects must use clearly different geometry and motion languages. The hero is a directional Decision Loom that changes from sensor array to reasoning braid to execution vector; the identity artifact remains the spherical Systems Kernel.
- Method is an interactive Validation Chamber rather than a row of principle cards: visitors should see evidence, control, infrastructure, and interface rules reorganize a live signal field.
- The identity thesis is “Not artificial intelligence. Accountable intelligence.” and should compile visibly from rejected language into the final accountable position.
- Experience, education, dates, links, and credential claims must remain grounded in `Aman_Kumar_Resume_ATS.pdf` and the supplied micro1 certificate. Strong writing is welcome; fabricated employers, dates, outcomes, or metrics are not.
- Keep the résumé at one canonical stable route with a small runtime manifest so Aman can replace the PDF or point the portfolio to a permanent external source without editing interface code.
- The preferred public deployment target is Vercel. Sites may remain a secondary preview, but future production handoffs should return the Vercel URL unless Aman says otherwise.
- Treat responsive quality as part of the authored experience, not a fallback: preserve the cinematic identity from 320px phones through tablet landscape and large displays, with safe-area-aware controls, 44px touch targets, no hover-only functionality, and deliberate short-landscape compositions.
- GitHub profile and repository surfaces should mirror the portfolio's evidence-first positioning, use the canonical Vercel and résumé URLs, and favor authored system graphics plus verifiable proof over generic badge walls.
- Public identity surfaces use authored evidence graphics and one canonical recruiter-safe, single-column ATS résumé. The portfolio identity section and GitHub may retain the non-human Systems Kernel, but LinkedIn uses Aman's supplied, identity-preserving editorial portrait paired with the Decision Loom banner. Do not promote a secondary résumé or visual dossier.
- The canonical flagship trio across the résumé and GitHub profile is Autonomous Personal Agent, AtlasLM, and MinePulse / KarixMC. Revive, AtlasForge AI, Distributed Search Typeahead, and GPT Prototype remain secondary proof rather than displacing the flagship narrative.
- Preserve the current cinematic portfolio world while making GitHub, LinkedIn, X / Twitter, the canonical résumé, email, and recruiter actions unmistakable within seconds. The approved global interaction is a coherent Signal Operating System: persistent professional-link dock, keyboard command center, recruiter quick-read, résumé manifest status, native sharing, and mobile action tray. Do not scatter unrelated effects around it.
- GitHub profile pins should prioritize the strongest recruiter-facing engineering proof in this order: Autonomous Personal Agent, AtlasLM (`notebooklm-rag`), MinePulse / KarixMC, Revive, ROLLFORWARD, and AtlasForge AI. Do not let introductory notebooks or coursework displace these six.
- LinkedIn uses Aman's supplied human portrait refined without changing his identity, clothing, expression, or recognizable features, paired with the complementary Decision Loom banner. It should use truthful recruiter-search keyword architecture, prominent live/source proof, and the canonical portfolio and resume links. Never publish LinkedIn posts or message contacts without Aman's explicit request.
- Profile headlines should lead with the actual engineering role and concise career context, including previous AI Engineer internship at micro1; avoid a long technology inventory in the headline. LinkedIn Featured should visibly include live Revive and KarixMC links in addition to the portfolio and strongest source proof.
- The next refinement uses the user-selected Engineering X-ray hybrid: real project interfaces reveal implementation layers, failure boundaries, and inspectable source evidence within the selected-work instrument. Blend cinematic inspection with an architect's workbench; keep the existing hero/descent identity and avoid a competing long pinned sequence. Preserve the recognizable natural portrait as the human introduction, not as proof of technical skill.
- The canonical résumé remains one-column and photo-free, with readable high-contrast text and source-grounded engineering bullets. Distinctiveness comes from specific decisions, constraints, and verifiable implementation, never visual parsing tricks, unsupported seniority, or undated test-count inflation.
